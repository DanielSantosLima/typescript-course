//npx parcel index.html === Code to run the file on the browser
//import { faker } from "@faker-js/faker";

import { Company } from "./Company";
import { User } from "./User";

const user = new User();
console.log(user);

const company = new Company();

console.log(company);
